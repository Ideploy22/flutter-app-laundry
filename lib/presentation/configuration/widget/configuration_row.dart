import 'package:flutter/material.dart';

class ConfigurationRow extends StatelessWidget {
  const ConfigurationRow({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
